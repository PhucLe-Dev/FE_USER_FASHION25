import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};
module.exports = {
  images: {
    domains: ['res.cloudinary.com', 'images.unsplash.com','assets.christiandior.com','www.dior.com',],
  },
};

export default nextConfig;
